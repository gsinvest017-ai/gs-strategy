# RAG + MCP extras (install into the crawler venv .venv)
#   .venv/bin/pip install -r requirements-rag.txt
# SQLite FTS5 (the vector/full-text store) is built into Python's stdlib sqlite3.
pypdf>=6.16.2      # PDF text extraction for rag-ingest
# Pinned <2 deliberately: quant_crawler/rag/mcp_server.py imports
# `mcp.server.fastmcp.FastMCP`, which mcp 2.x renamed to
# `mcp.server.mcpserver.MCPServer`. A dependabot PR relaxed this to
# >=2.0.0 without migrating the code, so any fresh install got
# ModuleNotFoundError at import; existing venvs kept working because
# they already had 1.x. Lift the pin only together with the migration.
mcp>=1.0,<2       # FastMCP server (quant_crawler.rag.mcp_server)
